\"\"\"Voice chat agent for Kral.\"\"\"\n\nfrom __future__ import annotations\n\nfrom agents.kral.turbo import TurboBrain\n\n\nclass KralVoiceChat:\n    \"\"\"Voice chat interface for Kral.\"\"\"\n    \n    def __init__(\n        self,\n        symbol: str = \"BTCTRY\",\n        bist_symbol: str = \"THYAO\",\n        turbo: TurboBrain | None = None,\n    ):\n        self.symbol = symbol\n        self.bist_symbol = bist_symbol\n        self.turbo = turbo or TurboBrain()\n        self.last_price: str | None = None\n        self.last_bist_price: str | None = None\n        self.stream_on = False\n    \n    def reply(self, text: str) -> str:\n        \"\"\"Generate reply to user input.\"\"\"\n        text_lower = text.lower()\n        \n        # Boost turbo on interaction\n        self.turbo.boost()\n        \n        # Response mapping\n        responses = {\n            \"merhaba\": f\"Selam! {self.symbol} ve {self.bist_symbol} takip ediyorum.\",\n            \"bitcoin\": \"Bitcoin (BTC) en buyuk kripto para. Anlık fiyat icin canli veri gerek.\",\n            \"ethereum\": \"Ethereum (ETH) akilli sozlesmeler icin platform.\",\n            \"fiyat\": f\"{self.symbol}: {self.last_price or 'yükleniyor...'}\\n{self.bist_symbol}: {self.last_bist_price or 'yükleniyor...'}\",\n            \"turbo\": f\"Turbo Level: {self.turbo.level}/10. Daha fazla sor!\",\n            \"yardim\": \"Sorabileceklerin: bitcoin, ethereum, fiyat, turbo, cikis\",\n            \"cikis\": \"Hosca kaldin!\",\n        }\n        \n        for keyword, response in responses.items():\n            if keyword in text_lower:\n                return response\n        \n        # Default\n        return f\"İlginç soru: '{text}'. Daha detaylı konuşabilir misin?\"\n    \n    def on_price(self, market: str, symbol: str, price: str) -> str | None:\n        \"\"\"Handle price update from stream.\"\"\"\n        if market == \"BIST\":\n            self.last_bist_price = price\n        else:\n            self.last_price = price\n        \n        # Only speak if turbo enabled stream voice\n        if self.stream_on and self.turbo.stream_voice_every_n() > 0:\n            return f\"{symbol} şu an {price}\"\n        \n        return None\n